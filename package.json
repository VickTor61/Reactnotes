{
  "name": "easynotes",
  "version": "0.0.1",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
